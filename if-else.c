#include <stdio.h>

int main() {
    int no;

    printf("Enter a number: ");
    scanf("%d", &no);

    if (no == 100)
    {
        printf("The number is 100.\n");
    } else
    {
        printf("The number is not 100.\n");
    }
  
}
